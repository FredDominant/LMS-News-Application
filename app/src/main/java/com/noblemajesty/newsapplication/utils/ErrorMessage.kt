package com.noblemajesty.newsapplication.utils

class ErrorMessage {
    companion object {
        const val NO_NETWORK_DETECTED = "no network"
        const val API_RESPONSE_ERROR = "api network error"
    }
}